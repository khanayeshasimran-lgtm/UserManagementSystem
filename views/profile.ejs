<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white gap-4 p-10">
        <a href="/logout" class="bg-red-600 text-white px-4 py-2 rounded-md hover:bg-red-600 cursor-pointer float-right">Logout</a>
        <h3 class="text-3xl mb-3"><span class="font-light">Welcome, </span><%= user.name %> 🎀</h3>
        <h5 class="mb-2 text-zinc-400">Create a new post.</h5>
        <form method="post" action="/post">
            <textarea class=" resize-none border px-3 py-2 rounded-md bg-transparent border-2 border-zinc-800 outline-none mb-3 w-2/3 h-20 block" name="content" placeholder="Write your post here..." rows="5"></textarea>
            <input type="submit" value="Upload Post" class="bg-blue-500 text-white px-4 py-2 rounded-md hover:bg-blue-600 cursor-pointer block">
        </form>
        <div class="posts mt-10">
            <h3 class="text-zinc-400">Yours Posts.</h3>
            <div class="postContainer mt-2">
                <% user.posts.reverse().forEach(function(post){ %>
                <div class="post w-2/3 bg-zinc-800 p-4 mb-2 border-[1px] border-zinc-800 rounded-md mb-2">
                    <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                    <p class="text-zinc-300 text-sm tracking-tight"><%= post.content %></p>
                    <small class="mt-5 inline-block"><%= post.likes.length %> likes</small>
                    <div class ="btns flex gap-4">
                        <a class="text-blue-300 text-sm" href="/like/<%= post._id %>">
                            <%= post.likes.includes(user._id) ? 'Unlike' : 'Like'%>
                        </a>
                        <a class="text-zinc-500 text-sm" href="/edit/<%= post._id %>">Edit</a>
                        <a class="text-red-500 text-sm" href="/delete/<%= post._id %>">Delete</a>
                    </div>
                </div>
                <%}) %>             
            </div>
        </div>
    </div>
</body>
</html>